```
npm install --global gulp-cli

gulp --version
```

https://gulpjs.com/docs/en/getting-started/creating-tasks

https://marketplace.visualstudio.com/items?itemName=ms-vscode.live-server

`_name.scss` are partials, they are not compiled to css, but imported in the main index.scss. They are used to organize the code and make it more maintainable.
